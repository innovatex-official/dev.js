{
  "name": "@devjs/project",
  "version": "0.1.0",
  "description": "Project loading, workspace discovery, and diagnostics for dev.js.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "clean": "rimraf dist *.tsbuildinfo",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "test": "vitest run src",
    "test:unit": "vitest run src",
    "bench": "tsx bench/project.bench.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@devjs/config": "workspace:*",
    "@devjs/core": "workspace:*",
    "yaml": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "rimraf": "latest",
    "tinybench": "latest",
    "tsup": "latest",
    "tsx": "latest",
    "typescript": "latest",
    "vitest": "latest"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/innovatex-official/dev.js.git",
    "directory": "packages/project"
  }
}
