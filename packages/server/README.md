# @devjs/server

Development HTTP server for dev.js applications.

## Features

- Loads a DevJS project.
- Discovers filesystem routes with `@devjs/router`.
- Renders route modules to HTML.
- Exposes `/__devjs/diagnostics`.
- Sends reload events through `/__devjs/events`.
- Renders a small error overlay for route and project failures.
